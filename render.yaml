services:
  - type: web
    name: taskflow
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c gunicorn_config.py app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
